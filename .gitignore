# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Experiment outputs are regenerated by experiments/run_all.py.
# results/ is committed on purpose: the figures are referenced from README.md
# and the JSON files are the record of what each run actually produced.

# Test/tooling caches
.pytest_cache/
.claude/

# The ADP paper (arXiv:1610.04271) is kept locally for reference, not committed.
1610.04271.pdf
